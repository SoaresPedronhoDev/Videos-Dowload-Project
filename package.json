{
  "name": "video-downloader",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "start": "tsc && electron .",
    "dev": "tsc --watch",
    "build": "tsc",
    "package": "electron-packager . VideoDownloader --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "dependencies": {
    "@distube/ytdl-core": "^4.16.12",
    "electron": "^37.3.0",
    "play-dl": "^1.9.7"
  },
  "devDependencies": {
    "@types/electron": "^1.6.12",
    "@types/node": "^24.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
